volumes:
  postgres-data:
    external: false

networks:
  swaga-sber-network:
    driver: bridge

services:
  db:
    container_name: swaga-sber-postgres
    image: postgres:18
    restart: always
    environment:
      - POSTGRES_DB=db
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=pass
      - POSTGRES_PORT=5432
    volumes:
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d db"]
      interval: 10s
      timeout: 5s
      retries: 3
    ports:
      - "5432:5432"
    networks:
      - swaga-sber-network

  server:
    container_name: swaga-sber-server
    build:
      context: .
      dockerfile: docker/Dockerfile.Server
    environment:
      - DOTNET_RUNNING_IN_CONTAINER=true
      - JWT_KEY=1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
    restart: always
    depends_on:
      db:
        condition: service_healthy
    networks:
      - swaga-sber-network
    ports:
      - "8080:8080"
